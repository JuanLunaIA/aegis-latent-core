import { LedgerPanel } from "@/components/LedgerPanel";

export default function LedgerPage(){return <><h1>Audit ledger</h1><p className="lede">Read-only, authenticated records from the primary gateway's retained memory window.</p><LedgerPanel/></>;}
