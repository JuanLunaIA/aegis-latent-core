# Aegis Latent Core — Public Claims Matrix

**Audience:** security reviewers, procurement, platform engineers, maintainers.
**Scope:** every public claim this project makes, its evidence state, and its boundary.
**Boundary:** this is a claim-control document, not a certification. It records what may be said and on what evidence; it does not itself establish assurance.

**Status:** `v4.1.2` source metadata is synchronized; SDK registry publication remains at `4.0.0` and target-environment acceptance requires independent evidence
**Canonical language:** US English
**Last verified:** 2026-09-02 UTC
**Release baseline:** four-layer truth model
**Source baseline:** checked-out source metadata is synchronized at `v4.3.0`, which is unpublished; the most recent published release is `v4.1.2`
**Immutable comparison source:** `fdace8844568eb788216740b2cb5daf187d99d3b` with `4.0.0` anchors
**External lifecycle boundary:** source metadata does not prove a tag, GitHub Release, registry package, OCI image, deployment, or acceptance; verify each surface by external readback
**Historical evidence baseline:** published `v3.1.0` artifacts and retained measurements; post-v3.1.0 capabilities are not attributed to that distribution
**Owner:** Release owner + qualified security reviewer
**Machine-readable ledger:** `evidence/market_hardening_v3_1/claims_ledger_v3_1_0.json` (generated outside the source tree)

This matrix separates what the repository implements, what has been measured in a named environment, what depends on deployment configuration, what remains roadmap work, and what requires legal or independent review. It is a claim-control document, not a certification. Unless a row explicitly cites historical distribution evidence, `IMPLEMENTED` describes the checked-out `v4.3.0` source baseline. It does not retroactively add that capability or result to the `v3.1.0` distribution. Source metadata alone does not establish a tag, GitHub Release, registry package, OCI image, deployment, or acceptance; those lifecycle facts require external readback. That readback was performed on 2026-09-04 and found `4.1.2` published on every surface. Rows citing a 2026-09-03 readback describe `4.1.1` and rows citing 2026-09-02 describe `4.0.2`; neither may be restated with the version changed.

## Product category

Aegis is an **OpenAI-compatible AI Governance and Evidence Gateway**. It sits between an application and an upstream model provider and applies request policy and security controls. For admitted non-streaming outcomes, it commits governed request/response evidence to a durable signed ledger before returning the response. For admitted SSE, it emits sanitized events incrementally and commits one exact-byte terminal summary before emitting the terminal marker. It is not an LLM, a universal WAF, a universal compliance product, a legal-admissibility decision, or a substitute for organizational security controls.

## Claim status vocabulary

| Status | Meaning |
|---|---|
| `IMPLEMENTED` | Source code and regression tests implement the behavior under declared conditions. |
| `MEASURED` | A reproducible artifact exists for a named workload, environment, sample method, and boundary. |
| `CONFIGURATION-DEPENDENT` | Behavior requires deployment controls such as Redis, durable storage, HSM/Vault, Seccomp, LSM, TLS, ingress, or a specific topology. |
| `ROADMAP` | Capability is incomplete, unmeasured, a stub, or future work. |
| `LEGAL-REVIEW-REQUIRED` | Statement could be interpreted as legal, regulatory, certification, procurement, or contractual claim. |

## Buyer-facing claims

| ID | Claim | State | Evidence locator | Boundary / falsification |
|---|---|---|---|---|
| `CLM-001` | Aegis exposes an OpenAI-compatible gateway surface. | `IMPLEMENTED` | `aegis/proxy/`, provider contract tests, README integration path | Falsified by a failed contract test or incompatible endpoint behavior. |
| `CLM-002` | Strict mode rejects startup or requests when required authentication, durable evidence, strong signing, body bounds, distributed limiting, or kernel controls are unavailable. | `CONFIGURATION-DEPENDENT` | `DEPLOYMENT_GUIDE.md`, `tests/test_p0_release_gates.py`, startup and failure-path tests | Target kernel, filesystem, Redis, signer, secrets path, and ingress must be validated at deployment. |
| `CLM-003` | Governed successful and terminal error responses expose durable evidence status. | `IMPLEMENTED` | `tests/test_enterprise_durable_evidence.py`, proxy failure-path tests, and v3.1.0 release evidence | Falsified by any accepted governed response without a durable evidence record in the declared test scope. |
| `CLM-004` | Admitted SSE streams incrementally emit sanitized canonical events through a bounded, byte-accounted queue; SHA-256 covers the exact emitted bytes, one terminal summary is committed to the WAL, and the terminal marker is emitted only after that commit succeeds. | `IMPLEMENTED` | `aegis/proxy/streaming.py`, stream integration in `aegis/proxy/app.py`, `tests/test_proxy_streaming.py`, and the per-stream arithmetic check in `specs/aegis_stream_buffer.smt2` | Initial headers report evidence and proof as `pending-terminal`; the inclusion proof is retrieved from the linked proof endpoint after terminal commit. Backpressure, queue-byte, queue-event, event-size, cumulative-output, de-identification-window, preview, and duration bounds apply per admitted stream; aggregate retained memory scales with concurrent admitted streams and therefore requires deployment-level admission/concurrency controls. |
| `CLM-005` | Portable MMR inclusion proofs can be generated from retained in-process MMR state and verified without local MMR state by the core and both SDK verifiers. | `IMPLEMENTED` in the v4 source baseline | `aegis/core/mmr.py`; `tests/test_mmr_portable.py`; `sdk/shared/mmr-inclusion-v1.json`; `sdk/python/tests/test_proof.py`; `sdk/typescript/tests/proof.test.ts` | Verification requires an independently trusted root and the declared `aegis-mmr-inclusion-v1` schema. Legacy records may have no portable proof; a valid proof establishes inclusion relative to that root, not external immutability, global ordering, or legal provenance. This capability post-dates the `v3.1.0` tag. |
| `CLM-006` | The Python and TypeScript SDK source trees provide tested gateway configuration, OpenAI/Anthropic integration, and portable-proof verification. | `IMPLEMENTED` in the v4 source baseline | `sdk/python/src/aegis_sdk/`; `sdk/python/tests/test_openai_client.py`; `sdk/python/tests/test_anthropic_client.py`; `sdk/python/tests/test_proof.py`; `sdk/typescript/src/`; `sdk/typescript/tests/dropin.test.ts`; `sdk/typescript/tests/gateway.test.ts`; `sdk/typescript/tests/providers.test.ts`; `.github/workflows/ci.yml` jobs `sdk-python` and `sdk-typescript` | Compatibility is limited to the provider surfaces, dependency ranges, and behaviors exercised by those tests; this is not a claim of compatibility with every provider version or endpoint. The source modules post-date `v3.1.0` and are not attributed to its release packages. |
| `CLM-007` | A bounded forensic export endpoint can produce a ZIP containing the contract-defined manifest, retained-window records, portable proofs where available, a technical PDF, and an offline embedded-file digest checker. | `IMPLEMENTED` in the v4 source baseline | `aegis/core/forensic_bundle.py`; `aegis/proxy/audit_api.py`; `tests/test_forensic_bundle.py`; `tests/test_audit_api_new.py::test_forensic_export_returns_verifiable_zip` | Export rejects empty or unbounded requests and is limited to records retained by the supplying process. The included `VERIFY.sh` checks file bytes against digest literals embedded in that same unauthenticated archive; co-tampering the file and script is therefore outside its detection boundary. It does not authenticate the archive, validate canonical semantics, verify signatures or MMR proofs, establish a trusted root, authorship, complete custody, certification, legal admissibility, or external immutability. This workflow post-dates `v3.1.0`. |
| `CLM-008` | The `dashboard/` application renders read-only gateway ledger, proof, health, metrics, and forensic-export views without synthesizing runtime records when data is empty or unavailable. | `IMPLEMENTED` in the v4 source baseline | `dashboard/src/`; `dashboard/tests/contracts.test.ts`; `dashboard/tests/no-fabrication.test.ts`; `dashboard/tests/states.test.tsx`; `.github/workflows/ci.yml` job `dashboard`; `evidence/commercial_phase2_dashboard_qa.md` | The UI depends on an authenticated reachable gateway and configured metrics source; explicit empty/unavailable/error states are not availability evidence. The local QA record is bounded to its stated environment and is not customer telemetry, capacity evidence, or independent assurance. The dashboard post-dates `v3.1.0`. |
| `CLM-009` | The ledger detects tampering or chain-link changes through canonical hashes and signatures. | `IMPLEMENTED` | `aegis/core/crypto_audit.py`, `verify_integrity()`, crypto tests | Integrity detection is not proof that an external storage system is immutable. |
| `CLM-010` | Finalized JSONL WAL segments can be represented by versioned manifests and asynchronously uploaded through an S3 Object Lock adapter with checksum, version, lock-mode, and retention verification. | `CONFIGURATION-DEPENDENT`; external availability requires readback | `aegis/storage/s3_worm.py`; `aegis/storage/segment_manifest.py`; storage tests | Only rotated segments are eligible. Local tests do not prove target-bucket versioning/Object Lock configuration, privileged-operator resistance, regulatory WORM status, lifecycle policy, recovery, cost bounds, or multi-cloud parity. |
| `CLM-011` | Exactly one process may append to a JSONL WAL path, on POSIX and on Windows. The ledger locks the WAL descriptor before publishing the handle — `fcntl.flock` on POSIX, `msvcrt.locking` over a one-byte sentinel region on Windows — so a second writer raises `WalWriterConflictError` at startup instead of forking the chain. | `IMPLEMENTED` in the v4 source baseline; the POSIX path is `LOCALLY TESTED` including cross-process, the Windows path is exercised by a `windows-2022` CI job | `aegis/core/crypto_audit.py` (`_lock_wal_fd`, `_unlock_wal_fd`, `WalWriterConflictError`); `tests/security/test_wal_single_writer.py`; `.github/workflows/ci.yml` (`windows-wal-lock`) | This prevents a second writer; it does not serialize two. It selects no predecessor and coordinates no MMR state, so multi-worker on one shared WAL remains unsupported rather than newly supported. The two primitives are not equivalent: `flock` is advisory and denies nothing to a process that ignores it, while `msvcrt.locking` is mandatory and denies the locked bytes to every other handle — which is why the Windows region is a sentinel past any record rather than the file head, so readers are not locked out. Neither constrains a writer reaching the same bytes by another path, and network filesystems carry their own lock semantics. On a platform offering neither primitive, or where the sentinel offset exceeds the filesystem maximum, the ledger warns and the discipline is operator-enforced. It is not a distributed lock and establishes nothing about cross-host or cross-pod ordering. |
| `CLM-012` | The Helm chart renders a `StatefulSet` with one `volumeClaimTemplates` claim per replica, pins `aegis.workers` to `"1"`, constrains `persistence.accessMode` to `ReadWriteOnce`/`ReadWriteOncePod`, and ships a default-deny `NetworkPolicy`. | `CONFIGURATION-DEPENDENT` | `deploy/helm/templates/statefulset.yaml`; `deploy/helm/templates/networkpolicy.yaml`; `deploy/helm/values.schema.json`; `tests/test_deploy_manifests.py` | Rendering a manifest is not a deployment. Each replica remains an independent evidence chain: this removes the shared-writer fork, it does not add cross-pod ordering, atomicity, or high availability. The `NetworkPolicy` is inert unless the cluster CNI enforces it, and its `ingressFrom`/`egressTo` peers ship empty by design, so a governed deployment must supply and accept them. Storage class, scheduler, zones, ingress, Redis, and recovery still require target acceptance. |
| `CLM-013` | `aegis_security_enforcement_mode` reports the loaded enforcement posture as `1` (strict) or `0` (development), with no labels and no configuration values. | `CONFIGURATION-DEPENDENT` | `aegis/proxy/app.py` (`create_app`); `aegis/core/observability.py`; `tests/security/test_enforcement_mode_metric.py` | It reports which mode the process loaded, not that strict-mode invariants passed. It requires the optional `metrics` extra and a working scrape; without `prometheus-client` no `/metrics` endpoint is registered and every metric is a no-op. `/health` deliberately does not carry it, because that endpoint contracts to leak no configuration. |
| `CLM-014` | RFC 3161 exchanges can be persisted and accepted only after nonce/imprint checks and OpenSSL verification against an explicit trust store. | `CONFIGURATION-DEPENDENT`; external availability requires readback | `aegis/anchoring/rfc3161.py`; anchoring tests | An obtained response is not a trusted timestamp. TSA selection, certificate/revocation/policy lifecycle, renewal, egress, and independent time trust require target acceptance. |
| `CLM-015` | The primary gateway can derive immutable tenant principals from configured API-key mappings, strict OIDC claims, and pinned mTLS identities; inference quotas use authenticated tenant and credential pseudonyms rather than session headers. | `IMPLEMENTED` in the v4 source baseline; `CONFIGURATION-DEPENDENT` for OIDC/mTLS/Redis | `aegis/auth/`; `aegis/proxy/dependencies.py`; `aegis/proxy/rate_limiter.py`; auth/rate tests | OIDC depends on the configured IdP/JWKS policy. Current mTLS source is explicit leaf-pinning plus verified transport provenance, not a general PKI/revocation engine. The alternate `aegis_server` surface remains a separate migration boundary. |
| `CLM-016` | Closed-schema security events can be projected to CEF, RFC 5424, Splunk JSON, or Datadog JSON through a bounded local spool. | `CONFIGURATION-DEPENDENT`; external availability requires readback | `aegis/telemetry/`; telemetry privacy tests | The schema excludes content fields and raw identity values, but downstream delivery, retention, parsing, access control, SLOs, and incident response are external. |
| `CLM-017` | The SIEM exporter exposes process-lifetime content-free acceptance, rejection, delivery, and retry counters plus a current durable-spool pending gauge. | `IMPLEMENTED` in the v4 source baseline | `aegis/telemetry/siem.py`; `tests/telemetry/test_otel_siem.py` | Aggregates do not contain payloads or identities and do not establish downstream receipt, retention, incident response, or an operational SLO. |
| `CLM-018` | A typed forensic query helper can apply exact metadata predicates and bounded pagination to a fixed tuple of retained-node references. | `IMPLEMENTED` as an unwired helper in the v4 source baseline | `aegis/forensics/search.py`; `tests/forensics/test_search.py` | It performs an in-process O(n) scan over supplied retained nodes only. It neither copies nor makes referenced node objects immutable. It is not full-text/regex search, a durable index, archived-WAL search, a global query API, or billion-scale performance evidence. |
| `CLM-019` | A machine-readable crypto capability report distinguishes implemented, optional-runtime, stub, and external-validation-required states. | `IMPLEMENTED` in the v4 source baseline | `aegis/crypto/capabilities.py`; `tests/crypto/test_capabilities.py` | The report labels the current ZK surface as a non-real stub, MMR proof growth as O(log n), and FIPS validation as absent; it is not a cryptographic certification or runtime attestation. |
| `CLM-020` | TEE discovery and attestation-policy evaluation fail closed without an injected evidence-authentication backend. | `IMPLEMENTED` as a source boundary in the v4 source baseline; hardware attestation remains `ROADMAP` | `aegis/core/tee_manager.py`; `aegis/core/attestation_capabilities.py`; hardware tests | Device-node visibility is not enclave loading or attestation. The repository has no SGX/SEV-SNP/TDX quote parser, vendor trust-chain/collateral validation, enclave execution evidence, host-root confidentiality evidence, or FIPS 140-3 validation. |
| `CLM-021` | The internal Laplace count primitive calibrates one release to sensitivity one for add/remove-one-record adjacency. | `IMPLEMENTED` as an internal primitive in the `v4.3.0` source baseline; no public API availability is claimed | `aegis/core/dp_analytics.py`; `tests/test_dp_analytics.py`; audit API regression tests | No DP analytics endpoint is published. There is no durable privacy accountant, stable dataset/query identity, repeated-release memoization, arbitrary aggregate sensitivity proof, or universal re-identification prevention. |
| `CLM-022` | The legacy HSM interface fails closed when PKCS#11 is unavailable and rejects ambiguous key selection. | `IMPLEMENTED` as source hardening in the v4 source baseline; target HSM use is `CONFIGURATION-DEPENDENT` | `aegis/core/hsm.py`; `tests/test_hsm.py` | Injected PKCS#11 mocks test adapter logic only. They do not establish vendor interoperability, private-key non-exportability, side-channel behavior, availability, certification, or a target deployment. |
| `CLM-023` | Fuzzing readiness requires both executables plus a private workspace, bounded parseable manifest, and confined regular file for every declared target; reporting contains no synthetic coverage or bug counts. | `IMPLEMENTED` as source honesty hardening; actual fuzz campaigns remain `ROADMAP` | `aegis/core/fuzzing_harness.py`; `aegis/core/attestation_capabilities.py`; fuzzing tests | The current tree has no cargo-fuzz workspace. It **does** carry Kani harnesses (`CLM-054`) and a Miri target (`CLM-085`); neither is a fuzz campaign, and this row claims nothing about either. This readiness probe does not establish Git provenance. No execution volume, measured coverage, absence of crashes/leaks/deadlocks, LLVM-level proof, or exhaustive result is claimed. |
| `CLM-024` | The repository includes a deterministic advisory AI context pack and navigation files. | `IMPLEMENTED` as documentation in the `v4.3.0` source baseline | `.aegis_ai_context/`; `AGENTS.md`; `CLAUDE.md`; `GEMINI.md`; `.github/copilot-instructions.md`; `llms.txt`; `tests/test_ai_context.py` | These files are advisory and cannot establish runtime behavior, override authorization, prove claims, or convert source metadata into evidence of an external release. |
| `CLM-025` | Release, Python/npm SDK, and OCI publication workflows are dispatch-only; protected-main control code requires a signed release tag plus its full target commit, verifies identity/issuer/ancestry, then builds only the exact signed source. The OCI path publishes gateway and dashboard multiarch images, attests their digests, and keyless-signs those digests. | `CONFIGURATION-DEPENDENT`; external availability requires readback | `.github/workflows/publish_pypi.yml`; `.github/workflows/publish_npm.yml`; `.github/workflows/publish_oci.yml`; `.github/workflows/release.yml`; `scripts/verify_release_contract.py` | Source inspection is not registry publication, trusted-publisher binding, release-environment acceptance, OCI execution, signature or attestation verification, or architecture smoke evidence; actual publication still requires a successful run and independent readback. The GitHub Release job is create-only but still requires external repository settings, trusted tag verification, permissions, and a successful run. The checked-out source and workflow definitions do not establish current tag, release, registry, or OCI publication; external readback is required. |
| `CLM-026` | The declared finite-state abstractions preserve commit-before-emission, append-only ledger prefixes, and session-to-ledger binding. | `MEASURED` | `scripts/verify_formal_artifacts.sh`, `specs/aegis_invariants.tla`, `specs/aegis_ledger_immutability.tla`, `specs/aegis_session_manager.tla`, `specs/AegisVerification.lean`, and `specs/aegis_invariants.smt2` | Falsified by a Z3 result other than `unsat`, a Lean type-check failure, or a TLC counterexample in the configured bounds. This is not a refinement proof of the Python/Rust implementation or target filesystem. |
| `CLM-027` | HMAC-SHA256, HSM/Vault, or a reviewed PQC signer can satisfy the configured signing policy. | `CONFIGURATION-DEPENDENT` | Signer configuration and tests | HMAC is symmetric and classical; it is not third-party non-repudiation or PQ resistance. |
| `CLM-028` | The enterprise HMAC signer can reload an atomic versioned keyring without restart. | `IMPLEMENTED` | `aegis_server/crypto/keyring.py`, `tests/test_keyring_rotation.py` | Three-replica propagation, secret-manager custody, clock behavior, and failure recovery require deployment evidence. |
| `CLM-029` | Compliance export bundles retain the non-secret signing key ID used for sealing. | `IMPLEMENTED` | `aegis_server/compliance/exporter.py`, exporter tests | Key ID metadata does not disclose key material or prove custody. |
| `CLM-030` | ML-DSA-65 is available through the native Rust dependency when the extension is present. | `CONFIGURATION-DEPENDENT` | `aegis/core/pqc_signer.py`, Rust tests, capability endpoint | Does not establish FIPS 140 validation, constant-time behavior, legal admissibility, or production availability on every platform. |
| `CLM-031` | The application-layer egress guard rejects malformed or unauthorized endpoint forms. | `IMPLEMENTED` | `aegis/proxy/egress_guard.py`, egress tests | Does not replace network namespaces, firewall policy, Kubernetes NetworkPolicy, or cloud egress controls. |
| `CLM-032` | The pinned WAF corpus recorded zero observed bypasses and zero false positives. | `MEASURED` | `tools/security/run_waf_corpus.py`, `tests/data/waf_corpus_v1.json`, `waf_corpus_report_v1_candidate.json` | 15 malicious and 8 benign local cases only; Wilson interval remains wide; observed rate is not universal detection coverage; HTTP/2 and Nuclei remain unexecuted. |
| `CLM-033` | The `<5%` WAF bypass threshold applies to any traffic or ingress. | `ROADMAP` | No universal artifact exists. | Threshold is meaningful only for a pinned corpus, ingress boundary, denominator, severity policy, and harness. |
| `CLM-034` | Under 2 ms injected fsync delay and 10k RPS offered load for 0.25 seconds, 2,500 offered requests produced 2,500 durable records with zero failures, zero missing/duplicate IDs, and valid chain integrity. | `MEASURED` | `tools/benchmarks/run_backpressure_stall.py`, `evidence/execution_2026-08-20/backpressure_stall_report.json` | Local injected seam only; observed p99 commit latency was 836.3514210795984 ms; offered load is not accepted capacity and does not prove target storage semantics. |
| `CLM-035` | Aegis accepts 10k requests/s in production. | `ROADMAP` | No target deployment capacity artifact exists. | Must include topology, upstream, storage, rejected traffic, latency distribution, and recovery. |
| `CLM-036` | The 2.70 µs result is a background-dispatch microbenchmark. | `MEASURED` | `docs/BENCHMARKS.md` | Must not be presented as end-to-end proxy overhead or provider-visible latency. |
| `CLM-037` | Cross-replica global audit ordering and multi-region HA are available today. | `ROADMAP` | `docs/performance/SCALING_GUIDE.md`, `docs/ROADMAP.md` | Current topology provides independently verifiable per-replica evidence unless a centralized writer is deployed. |
| `CLM-038` | Static Samples dashboards represent live runtime, customer activity, cryptographic proof, or production capacity. | `ROADMAP` | `Samples/` plus explicit demo banner and bootstrap metadata | Static dashboards are illustrative only; sample values must not be used as evidence. |
| `CLM-039` | Aegis implements technical controls that may contribute evidence to customer governance, privacy, security, retention, and policy workflows. | `LEGAL-REVIEW-REQUIRED` | `docs/compliance/COMPLIANCE_MAPPING.md`, `docs/privacy/DATA_RETENTION.md`, and the external source register | Not a SOC 2 opinion, HIPAA determination, FedRAMP authorization, EU AI Act conformity assessment, GDPR legal basis, or legal advice. |
| `CLM-040` | Aegis provides constant-time ML-DSA signing or verification. | `ROADMAP` | `docs/security/PQC_CONSTANT_TIME.md`, `pqc_timing_report_v2.json` | The retained v3.1.0 1M-sample measurement met non-detection for `sign` (`p=0.8521504207157158`) but failed the declared `verify` threshold (`p=0.0`); no constant-time claim is approved. |
| `CLM-041` | The open-source project has an enterprise support SLA, customer references, or independent assurance. | `ROADMAP` | No independent evidence in the repository. | Do not imply 24/7 support, SOC 2, pentest completion, or procurement readiness without artifacts and accountable operations. |
| `CLM-042` | Published pricing hypotheses are market-validated Aegis prices. | `ROADMAP` | `docs/COMMERCIAL_STRATEGY_US.md` | Ranges are illustrative hypotheses pending buyer interviews, cost-to-serve modeling, quotes, and paid pilots. |

## Durability, proof and evidence-transport claims

| ID | Claim | State | Evidence locator | Boundary / falsification |
|---|---|---|---|---|
| `CLM-043` | A committed node is written, flushed, and `fsync`-ed before the governed response returns. | `IMPLEMENTED` | `aegis/core/crypto_audit.py` (`_persist_node`, lock→node→sign→write→flush→fsync ordering); `tests/test_enterprise_durable_evidence.py` | `fsync` returns when the filesystem reports the write reached stable storage. It is not power-loss durability: a device with a volatile write cache that acknowledges early can lose an `fsync`-ed record. Power-loss durability requires storage with power-loss protection, which the repository cannot verify. See [Storage Requirements](operations/STORAGE_REQUIREMENTS.md). |
| `CLM-044` | Verifying an MMR inclusion proof requires a trusted root obtained independently of the gateway that served the proof. | `IMPLEMENTED` | `aegis/core/mmr.py`; `sdk/shared/mmr-inclusion-v1.json`; `sdk/python/tests/test_proof.py`; `sdk/typescript/tests/proof.test.ts` | A proof verified against a root supplied by the same gateway establishes internal consistency only, not independent evidence. The proof is not zero-knowledge: it discloses the leaf. It establishes inclusion under that root and nothing about confidentiality, identity, time, custody, consensus, non-membership, or external anchoring. |
| `CLM-045` | The JSONL WAL is the authoritative evidence store; the native Rust streaming WAL is auxiliary. | `IMPLEMENTED` | `aegis/proxy/app.py` (`native_stream_wal`, `_commit_stream_evidence`); `aegis/core/observability.py` (`aegis_native_stream_wal_errors_total`) | When the Rust extension is absent or its append fails, the gateway logs and continues because the JSONL WAL already holds the authoritative record. Do not describe the native WAL as the evidence store, and do not treat its absence as evidence loss. |
| `CLM-046` | An admitted SSE stream reports evidence and proof as `pending-terminal` until its terminal summary commits. | `IMPLEMENTED` | `aegis/proxy/streaming.py`; `tests/test_proxy_streaming.py::test_success_hashes_exact_output_and_commits_before_done` | `pending-terminal` is not a durability claim about the events already emitted. It states that the terminal summary has not yet committed, so no inclusion proof exists yet for that stream. A client that stops reading before the terminal marker has no durable evidence record for the stream. |
| `CLM-047` | Deterministic pattern-based redaction removes matched PHI and cardholder-data patterns from governed payload fields. | `IMPLEMENTED` | `aegis/core/phi_deidentifier.py`; `aegis/core/pci_detector.py`; `aegis/proxy/app.py` (payload scrubbing visitor) | Best-effort and pattern-bound. It matches the patterns it implements over the fields it visits. It does not detect free-text disclosure, paraphrase, indirect identifiers, novel formats, or non-English identifiers, and it does not remove data already sent to the upstream provider. See [PII Redaction Boundaries](privacy/PII_REDACTION_BOUNDARIES.md). |
| `CLM-048` | The core engine `aegis-latent-core` and the SDK `aegis-latent-sdk` are published on PyPI at `4.1.2`, and `aegis-latent-sdk` is published on npm at `4.1.2`. | `MEASURED` by readback, 2026-09-04 | [Release Status §1.0, §2.4, §2.5](RELEASE_STATUS.md) | `4.1.2` is the first version at which the gateway reached PyPI; at `4.1.1` and earlier `aegis-latent-core` returned HTTP 404, so any statement that the registries carry SDKs only is a `4.1.1`-and-earlier statement and must not be restated as current. The npm version history skips `4.1.1` and `4.0.2` entirely. Publication is not verification of build provenance: the two PyPI `aegis-latent-core` artifacts are byte-different from the release assets of the same name — identical content, different ZIP creator-system field — so the release `SHA256SUMS` and attestation do not cover the PyPI gateway downloads. The SDK artifacts on both registries do match `SHA256SUMS`. |
| `CLM-049` | GitHub Release `v4.1.2` exists with 31 uploaded assets and a published `SHA256SUMS`, and OCI gateway and dashboard images `4.1.2` are present in GHCR with cosign signature objects. | `MEASURED` by readback, 2026-09-04 | [Release Status §1.0, §2.3, §2.6, §2.7, §2.8](RELEASE_STATUS.md) | A release envelope and a pushed image are not registry publication of the SDKs. A present signature object is not a verification: `cosign verify` and `gh attestation verify` were not run for `4.1.2`. The full `sha256sum --check --strict` sweep was not run for `4.1.2` either — three artifacts were hashed individually — so asset integrity is established for those three and asserted by the published list for the rest; the `4.1.1` byte check in §1.1 covers `4.1.1` only. Per-platform digests under each `4.1.2` index were not re-read. |
| `CLM-050` | GitHub reports this repository's Sigstore-signed tags as unverified with reason `bad_cert`, which is the expected result for a short-lived certificate. | `IMPLEMENTED` as a documented provenance property | [Release Status §3](RELEASE_STATUS.md); `.github/workflows/create_release_tag.yml` | Observed on `v4.0.2`, and again on `v4.1.2` read back 2026-09-04: `verification.verified: false`, `verification.reason: bad_cert`, with the signed message and Fulcio certificate present. `git verify-tag` reports the matching missing-issuer condition under `gpgsm`. The badge is not evidence of compromise and is also not evidence of authenticity. Neither conclusion follows from it. Trust requires `gitsign verify` against the transparency log with an explicit certificate identity and OIDC issuer. |
| `CLM-051` | Aegis provides a production SLO, uptime guarantee, or capacity commitment. | `ROADMAP` | No target deployment measurement or executed agreement exists in the repository. | Availability and latency targets require a named topology, a measurement method, an agreed denominator, and a contract. Repository benchmarks are local observations and are not service levels. |
| `CLM-052` | Finalized WAL segments are stored under write-once-read-many controls. | `ROADMAP` | `aegis/storage/s3_worm.py` provides an S3 Object Lock adapter only. | The adapter can target a bucket configured for Object Lock and verifies checksum, version, lock mode and retention on upload. It does not configure, enforce, or attest the bucket's retention policy. Whether the target satisfies a regulatory WORM definition is a determination for the customer and their assessor. |
| `CLM-053` | Rotation makes archived WAL segments immutable. | `ROADMAP` | `aegis/core/crypto_audit.py` (segment rotation, `0o600`) | Rotation renames the segment and applies owner-only permissions. That is access restriction, not immutability: a privileged actor can still alter or delete the file unless an external storage control prevents it. |
| `CLM-054` | The native WAL's frame-bounds arithmetic is model-checked over all `usize` inputs. | `IMPLEMENTED` | `mod verification` in `aegis_rust_v2/src/wal.rs`; Kani 0.67.0; `Kani Model Checking` CI job; [Formal Verification §Kani](formal/FORMAL_VERIFICATION.md#kani-bit-level-model-checking) | Covers `header_range` and `payload_range` only. Kani models no `mmap`, no `flush_range`, no filesystem and no concurrency, so this establishes nothing about durability, crash consistency, power-loss behaviour, or that `capacity` equals the mapped length. It does not make the WAL memory-safe as a whole and is not a whole-system refinement proof. |
| `CLM-055` | A failed commit leaves the MMR exactly as it was, at a cost independent of chain length. | `IMPLEMENTED` | `MerkleMountainRange.checkpoint()` / `rollback_to()`; `tests/test_mmr_rollback.py`; `tests/test_crypto_audit_rollover.py` | The rollback token is O(log N) and exact, verified against a `deepcopy` restore. It is a correctness and cost property of one in-process structure; it establishes no throughput figure and no durability property. |
| `CLM-056` | Exactly one process may append to a WAL path, enforced rather than documented. | `IMPLEMENTED` | `aegis/core/crypto_audit.py` (`_lock_wal_fd`, `WalWriterConflictError`) | POSIX advisory locking via `fcntl.flock`. Advisory locks bind cooperating processes on the same host and the same filesystem; they do not constrain a writer that bypasses the code path, a second host on shared storage, or an NFS mount where `flock` semantics differ. On platforms without `fcntl` the guard logs a warning and discipline remains operator-enforced. |
| `CLM-057` | Streaming redaction fails closed only on candidates the detectors could still match. | `IMPLEMENTED` | `aegis/core/streaming_deidentifier.py`; `tests/test_proxy_streaming.py`; [PII Redaction Boundaries §3](privacy/PII_REDACTION_BOUNDARIES.md#streaming-the-bounded-holdback-and-its-fail-closed-edge) | Each open-candidate check is a viable-prefix test against the detector that would redact the text. The `ADDRESS` pattern's street-name run is bounded at 40 characters and the guard mirrors that bound, so a viable candidate (at most 46 characters) always fits the 64-character minimum window and the abort is structurally unreachable. The cost is recall: a street name longer than the bound is no longer redacted, in the streaming and non-streaming paths alike, and `tests/test_phi_address_bound.py` asserts that cost directly. Redaction remains best-effort pattern matching; see `CLM-024`. |
| `CLM-058` | Reopening a native WAL segment with a smaller capacity preserves committed frames. | `IMPLEMENTED` | `RustWal::open` in `aegis_rust_v2/src/wal.rs`; `reopening_with_a_smaller_capacity_does_not_truncate_the_segment` | The requested capacity is a floor; a segment only grows. This is a data-preservation property of the auxiliary segment on open. It does not change the segment's auxiliary role (`CLM-045`) and establishes no durability guarantee. |
| `CLM-059` | Governed requests are refused while the evidence chain is known to be broken. | `IMPLEMENTED` | `_require_intact_ledger` in `aegis/proxy/app.py`; `tests/test_app_wal_corrupt.py`; [Failure Semantics §4](architecture/FAILURE_SEMANTICS.md) | Scoped to the JSONL WAL. Triggered by the ledger's own `_fault_state`, which is set when startup replay stops at a malformed line **and, since `CLM-082`, when a commit-time `fsync` fails** — the second case means the WAL may be missing records that were never reported as missing. It says nothing about the enterprise storage providers, whose concurrent-append guard is `CLM-081` — a chain that forked there is not a `_fault_state` this check would see. It does not detect corruption that replay parses successfully, corruption introduced after startup, or tampering outside the WAL. `/health` and `/metrics` deliberately remain reachable while traffic is refused. |
| `CLM-060` | A request refused before admission is committed to the signed evidence chain as a node with `status="rejected"`. | `IMPLEMENTED` | `commit_rejection` in `aegis/core/crypto_audit.py`; `_commit_rejection_evidence` in `aegis/proxy/app.py`; `tests/test_pre_admission_rejection.py` | The request body is hashed, never stored. The refusal is unconditional: an evidence failure still refuses the request and reports `X-Aegis-Evidence-Status: rejection-uncommitted`, so the header's absence of `durable-rejection` is the only reliable signal that a node exists. A faulted ledger is not extended. Wired at the WAF and rate-limit paths; it is not a claim that every possible refusal in the stack is recorded. |
| `CLM-069`–`CLM-072` | Offline commercial licensing, modular engine facades, enterprise connectors, Envoy WASM filter | Do not use: "metered usage"; "enforced transaction cap"; "phone-home"; "the free tier is feature-limited" — enforcement is off by default and every engine is importable unlicensed; "node-locked" or "copy-protected" — a token is a bearer credential and offline licensing cannot bind to a machine; "revocable" — there is no CRL or OCSP; "the engines add a capability" — they are facades over existing core modules and add none; "evidence in Splunk/Snowflake" — connector output is a derivative copy and verification is against the WAL; "the WASM filter governs and audits" — it commits no evidence at all | 2026-09-09 | Security owner + Commercial owner |
| `CLM-073`–`CLM-076` | Dependency triage, licence inventory, digest-stack migration, adversarial hardening | Do not use: "the dependency set is free of vulnerabilities" or "zero CVEs" — the snapshot reports what OSV held on one date; "audited", "certified" or "SLSA Level 3" — nothing here is signed and no external party reviewed it; "SBOM attested" — the SBOMs carry no provenance binding; "legally compliant" or "licence-clean" as a legal conclusion — the inventory reports declared identifiers and counsel owns the judgement; "fuzzed to saturation" — the campaigns were bounded by session time; "the Rust code is fuzzed" — `cargo-fuzz` could not be installed; "we fixed CVE-…" for block-buffer — the advisory carries **no CVE identifier**, so cite it as the scanner row and its CVSS, never as a CVE number; "the crate is now sound" — three panic shapes pass Miri, which is not the same claim; "ReDoS-free" — one quadratic pattern was found and fixed, which is not a proof there are no others | 2026-09-09 | Security owner |
| `CLM-077`–`CLM-079` | Licence model separation, per-stream retained-byte ceiling, public-surface compatibility | Do not use: "expiry is tamper-evident" or "the clock is verified" — injecting `now` makes the boundary testable and nothing more; "the model has no crypto dependency at import time" — the package re-export loads one; "memory-bounded" or "constant memory" without "per stream" — aggregate footprint scales with concurrency; "Z3 proves the proxy's memory use" or "formally verified memory safety" — the check is arithmetic consistency over declared ranges; "the in-process engine is within the verified domain" — `Q`, `E` and `P` are zero there, which the spec's ranges do not cover; "API-stable" or "guaranteed backward compatible" — the suite pins names, routes and call shapes, not behaviour or response bodies; "v4.2.0 compatibility" — no `4.2.0` exists to be compatible with | 2026-09-10 | Architecture owner + Commercial owner |
| `CLM-080`–`CLM-080` | Cross-replica gossip reconciliation | Do not use: "consensus", "Raft", "Paxos", "global ordering", "multi-pod linearizability", "Byzantine fault tolerant", or any comparison against a consensus protocol — anti-entropy reconciles ordering, not honesty, and a replica that contributes fabricated leaves has them merged like any other; "the ledger is reconciled across replicas" or "one audit chain across the cluster" — it reconciles the CRDT accumulator and each WAL stays its own chain (`CLM-012`); "self-healing cluster" or "automatic failover" — there is no membership protocol, no leader and no failover, only a static peer list an operator maintains; "multi-region" or any convergence-latency figure — convergence was observed over three replicas on loopback TLS, which is not a capacity, topology or latency result; "authenticated evidence" on the strength of the mesh's mutual TLS — it establishes that a peer holds an operator-issued certificate and nothing about what that peer sends | 2026-09-10 | Security owner + Architecture owner |
| `CLM-081`–`CLM-081` | Concurrent append fork prevention | Do not use: "the audit chain cannot fork" without naming the backend — only SQLite is verified here and the JSONL WAL is a different path entirely; "distributed consensus", "linearizable", or "multi-writer safe" — a compare-and-append rejects a loser, it does not order writers; "no evidence is lost under concurrency" — an append refused by another process is not retried and does not land; "PostgreSQL and DynamoDB are tested" — neither was executed in this environment; "forks are detected" — the constraint prevents them, and a store that forked before it existed is refused rather than repaired | 2026-09-10 | Security owner + Architecture owner |
| `CLM-082`–`CLM-082` | Coalesced WAL group commit | Do not use: "faster durability" or "durability without the fsync cost" — every commit still waits for an fsync, the cost is shared rather than removed; the measured throughput or p99 figures as a capacity, SLA or production-readiness statement — fsync cost is a property of the device under the run, not of this code; "no data loss" or "crash-proof" — fsync is a request to the operating system, and hardware that acknowledges before the platter does defeats it exactly as it defeated the per-record version; "batches are capped at 64" — the size knob bounds how long a syncer waits for company, not what an fsync covers; "nodes are durable before they enter the chain" — that ordering was true of the old per-record fsync and is not true now (`CLM-082` states what replaced it) | 2026-09-10 | Architecture owner + Security owner |
| `CLM-083`–`CLM-083` | Cluster receipt verification | Do not use: "the cluster verified it", "cluster consensus" or "corroborated by the cluster" — a match is one replica's ledger, and replicas do not attest to each other's; "the mesh verifies receipts" — gossip reconciles the accumulator, whose root cannot verify a receipt at all; "quorum" or "N-of-M" — no threshold is computed and none is available; "roots can be read from the cluster API" — roots taken from the receipt's own supplier establish internal consistency only, exactly as for a single-replica receipt | 2026-09-10 | Architecture owner + Security owner |
| `CLM-084`–`CLM-084` | ML-DSA backend seam | Do not use: "Aegis has migrated off PQClean" — the default build still uses it and the swap is an opt-in build flag; "the pure-Rust backend is faster" or "more secure" — it is 1.43x slower to verify, 4.80x slower to sign, and shows a larger verify class delta; "the advisories are resolved" — they are resolved only in a build that takes the flag, and they were never defects; "both implementations are constant-time" or "the timing gap is closed" — the block in `PQC_CONSTANT_TIME.md` stands for both backends and both operations; "ML-DSA verification is constant-time because it handles no secret" — handling no secret is why the verify experiment is not a side-channel measurement, which is not the same as a constant-time property | 2026-09-10 | Security owner + Architecture owner |
| `CLM-085`–`CLM-085` | Undefined-behaviour checking | Do not use: "the Rust core is free of undefined behaviour" or "memory-safe, verified" — Miri observes only the paths a test drives, and it drives one target; "the WAL is checked for UB" — Miri cannot service `mmap`, so the WAL is unreachable under it and is covered instead, and only for frame arithmetic, by `CLM-054`; "the cryptographic code is checked for UB" — the default build links PQClean C, which Miri cannot execute at all; "formally verified" — Miri is an interpreter, not a prover | 2026-09-10 | Release owner + qualified formal-methods reviewer |
| `CLM-086`–`CLM-086` | TEE attestation backends | Do not use: "Aegis supports Nitro or SEV-SNP attestation", "hardware-attested", "confidential computing" or "runs in a TEE" — both platform adapters refuse unconditionally and no vendor verifier is integrated; "attestation is verified in tests" — the passing test replays a fixture and authenticates nothing; "the device is present, so the enclave is attested" — device presence is discovery evidence and the code refuses precisely there | 2026-09-10 | Release owner + qualified security reviewer |
| `CLM-087`–`CLM-087` | Hybrid post-quantum key exchange | Do not use: "PQC TLS", "PQC TLS ingress", "quantum-safe TLS", "post-quantum TLS termination" — there is no TLS anywhere in this module and ingress TLS is the customer's; "X25519MLKEM768" — the implementation is ML-KEM-1024 and the group name appears only as an analogy; "FIPS-validated" or "constant-time ML-KEM" — the backend is pure Python and no such property is claimed; "quantum-safe by default" — the exchange is inert without the `pqc` extra and refuses rather than downgrading | 2026-09-10 | Release owner + qualified cryptographic reviewer |
| `CLM-088`–`CLM-088` | Recorded WAF verdict | Do not use: "the gateway proves the request was safe", "WAF-verified", "attested safe" — the field records that the WAF allowed the request, not that the WAF is correct or that the payload was benign; "every record carries a verdict" — nodes written before the field, and every non-proxy commit path, carry none, and empty means not recorded; "the tree proves the verdict" — recording it makes a proof *possible*, and no such proof exists in this tree yet | 2026-09-10 | Release owner + qualified security reviewer |
| `CLM-064`–`CLM-068` | MMR domain separation, tenant identifier comparison, causal-Merkle CRDT, streaming grammar frontier, cryptographic erasure | Do not use: "every chain uses domain-separated hashing" or "v2 is enabled" without "for new chains" — `auto` starts a new chain on v2 and reopens an existing one under the scheme its WAL recorded, so v1 chains in the field stay v1 and there is no in-place upgrade; "tenant isolation was broken"; "a cross-tenant read was fixed"; "constant-time" without naming the experiment; "global ordering"; "multi-pod linearizability"; "Byzantine fault tolerant"; "consensus" — the CRDT is an unwired data structure and reconciles ordering, not honesty; the frontier automaton now runs by default but matches four declared patterns **in addition to** the twenty Safe Harbor detectors, so never "provably safe streaming", "blocks prompt injection", or "the grammar frontier replaced the de-identifier"; the shredder is off by default and destroys a key, so never "GDPR compliant", "right to erasure satisfied", "data destroyed", "sanitised", or "Aegis shreds evidence on request" — it is opt-in, it cannot be enabled on an existing chain, it makes plaintext unrecoverable to a ciphertext holder, it says nothing about the medium, and the node keeps its request and response digests; and the tenant comparison changed the shape of a denial, not who could read what | 2026-09-10 | Security owner + Architecture owner |
| `CLM-061` | The embedded engine applies the gateway's WAF, redaction and signed ledger inside the calling process. | `IMPLEMENTED` | `aegis/embedded.py`; `tests/test_embedded_mode.py` | It governs calls made through the client it wrapped and nothing else. Code in the same process can call the provider directly, hold an unwrapped client, or edit the WAL, so this is an evidence and policy layer for cooperative code — **not** a containment boundary against the process it runs in, and not a substitute for the gateway where the application is itself the thing being constrained. Provider SDKs are recognised by shape and are not dependencies. |
| `CLM-062` | An A2A receipt shows a third party that an execution was recorded, without disclosing arguments or results. | `IMPLEMENTED` | `aegis/core/a2a.py`; `sdk/python/src/aegis_sdk/a2a.py`; `sdk/typescript/src/a2a.ts`; `tests/test_a2a_protocol.py` | A valid receipt establishes inclusion of its canonical envelope under the root supplied by the verifier, and nothing more. It does **not** establish that the tool ran, that either agent identifier is authentic, that the caller was authorised, or that the timestamp is accurate — that is the issuer's unattested clock. The root must be obtained independently of the receipt's supplier. Arguments appear as SHA-256 digests, which is not a commitment scheme: a low-entropy input can be recovered by enumeration. |
| `CLM-063` | The MMR accumulator can be restored from a persisted peak set in O(log N) instead of replaying every leaf. | `IMPLEMENTED`, off by default | `restore_from_peaks` in `aegis/core/mmr.py`; `_save_mmr_state`/`_load_mmr_state` in `aegis/core/crypto_audit.py`; `tests/test_mmr_state_continuity.py` | An optimisation over the WAL, never a substitute for it: the restored root is accepted only when it equals the root the last committed node recorded, and any missing, stale, corrupt or disagreeing checkpoint falls back to full replay. It is opt-in (`mmr_fast_restore=True`) because a peak restore summarises the leaves below it, so their inclusion proofs can no longer be derived in memory; each committed node still carries its own `mmr_proof`. This is not a cross-restart continuity claim — that property predates it and is covered by `tests/test_mmr_restart.py`. |
| `CLM-064` | Extending `CLM-006`, the `aegis-mmr-inclusion-v2` scheme applies RFC 6962-style domain separation — leaf `SHA-256(0x00 \|\| payload)`, node `SHA-256(0x01 \|\| left32 \|\| right32)`, root `SHA-256(0x02 \|\| peaks)` — so a leaf payload cannot hash to an interior node. `v1` proofs continue to verify unchanged. | `IMPLEMENTED` and `LOCALLY TESTED` in the core and both SDK verifiers | `aegis/core/mmr.py` (`v2_leaf_hash`, `v2_node_hash`, `v2_bagged_root`); `tests/test_mmr_domain_separation.py`; `sdk/python/src/aegis_sdk/proof.py`; `sdk/typescript/src/proof.ts`; `sdk/typescript/tests/mmrV2.test.ts` | v2 is **selectable** on `CryptographicAuditLedger` through `mmr_hash_scheme` (`AEGIS_MMR_HASH_SCHEME`) and is now the default **for a new chain**: the setting defaults to `auto`, which starts a new chain on v2 and reopens an existing one under whatever scheme its WAL recorded. A plain v2 default would refuse every chain already in the field. Moving the default surfaced four latent v2 defects — a hardcoded v1 leaf digest in `aegis/core/a2a.py` and in both SDK verifiers, a hex-only `trusted_root` check that rejected the base64url form a v2 proof transports, and an `X-Aegis-MMR-Format` header pinned to the literal `aegis-mmr-inclusion-v1` — all fixed here. **Wire boundary:** a receipt from a v2 chain does not verify against an SDK build predating those fixes, including published `4.1.2`, so gateway and SDKs must move together. Both blockers are cleared: `aegis_rust_v2/src/mmr.rs` implements both schemes under the same names, with Rust↔Python root agreement asserted across a 37-leaf rollover for each, and the migration rule is that **a scheme belongs to a chain**. There is no in-place upgrade and cannot be one — a root cannot be recomputed under a different construction without rewriting the history it commits to — so the ledger reads the proof version its WAL recorded and refuses to reopen it under a different scheme with fault state `mmr_scheme_mismatch`, rather than replaying to a different root and reporting intact evidence as corrupt. Selecting v2 means starting a new chain, and the v1 chains a deployment already holds stay verifiable under v1 forever. The v1 weakness this closes is reachable through `verify_portable_inclusion`, which accepts caller-supplied leaf bytes; it is not a break of SHA-256 and does not let an attacker forge a proof against a root they do not control. Domain separation establishes leaf/node type distinctness and nothing further — not confidentiality, identity, time, custody, consensus, non-membership, or external anchoring. |
| `CLM-065` | Extending `CLM-015`, tenant identifiers are compared with `hmac.compare_digest` over their UTF-8 bytes, so the comparison is defined for every string rather than only for ASCII. A client-supplied `tenant_id` carrying non-ASCII yields the boundary's `403 Tenant access denied`, and two credentials naming one internationalized tenant combine instead of failing. | `IMPLEMENTED` in the v4 source baseline; `LOCALLY TESTED` | `aegis/proxy/dependencies.py` (`_same_tenant`, `_combine`, `principal_tenant`); `aegis/auth/mtls.py`; `tests/security/test_tenant_isolation.py` | This is an error-handling and availability correction, not a new isolation guarantee, and it closed no cross-tenant read: `hmac.compare_digest` raised `TypeError` on non-ASCII `str`, so the previous behaviour denied the request as an unhandled 500 rather than returning another tenant's evidence. What changed is that a denial is now recorded as the intended 403, and that a legitimate non-ASCII tenant is no longer locked out. Identifiers are compared as exact UTF-8 bytes and are deliberately not Unicode-normalized, so two canonically equivalent spellings stay distinct tenants and deny; normalizing would let one tenant resolve to another. The platform `ADMIN` role may still select any tenant by design (`CLM-015`). On the mTLS surface the expected-tenant comparison is reachable only for direct callers of `aegis.auth.mtls`, since the bundled gateway calls `verify()` without a `tenant_id`; a certificate cannot supply a non-ASCII tenant, because `cryptography` rejects non-ASCII DNSName, RFC822Name and URI SANs at construction. Encoding to bytes preserves whichever timing property `hmac.compare_digest` supplies and adds none: no timing experiment was run here, and the comparison still reveals length. It establishes nothing about storage-level isolation, per-tenant encryption, or the integrity of an external identity provider's tenant claim. |
| `CLM-066` | `CausalMmr` in `aegis_rust_v2/src/crdt_mmr.rs` is a join-semilattice over domain-separated Merkle Mountain Ranges: `join` is idempotent, commutative and associative, so replicas that exchange leaf sets converge on one root regardless of the order they merge in. | `IMPLEMENTED` as an unwired accumulator in the v4 source baseline; `LOCALLY TESTED` | `aegis_rust_v2/src/crdt_mmr.rs` (`VectorClock`, `CausalLeaf`, `CausalMmr::join`); its Rust `mod tests`; `tests/test_crdt_mmr.py` | It is a data structure, and `CLM-080` now supplies the transport that runs it; it remains **not wired into `CryptographicAuditLedger`**, which writes its own roots, and there is still no membership protocol or persistence for it — so this establishes no cross-replica ordering *for the ledger*, and the independent-chain limitation in `CLM-012` is unchanged. Convergence is not agreement about truth: `join` reconciles replicas that disagree about *ordering*, not replicas that lie, and a replica contributing fabricated leaves has them merged like any other. It establishes nothing about Byzantine resistance, gossip liveness, network partitions, replica authentication, or bounded convergence time. The laws are asserted by tests over generated replica sets, not by a mechanized proof. Vector-clock counters saturate rather than wrap at `u64::MAX`. |
| `CLM-067` | `GrammarFrontierAutomaton` redacts a declared pattern that straddles a streaming chunk boundary. It redacts every complete match in the buffer to fixpoint and only then computes the withheld frontier over the residual text, and each rule's frontier is at least the longest string its own pattern can match. | `IMPLEMENTED` and wired into the governed streaming path; `LOCALLY TESTED`; overhead `MEASURED` on 2026-09-10 | `aegis/core/streaming_safety_engine.py`; `aegis/core/stream_redactor.py`; `aegis/proxy/streaming.py`; `tests/test_streaming_safety_engine.py`; `tests/test_streaming_safety_engine_integration.py`; `evidence/streaming-engine/4.3.0/` | It runs **in addition to** `StreamingDeidentifier` (`CLM-057`), not instead of it: the automaton declares four rules and the Safe Harbor set declares twenty, so replacing one with the other would remove eighteen detectors — `EMAIL`, `ADDRESS`, `MRN`, `URL`, `TRACK_DATA`, `CVV` and the Luhn-validated `PAN` among them — from the evidence path. Two of the automaton's rules, `INSTR_OVERRIDE` and `SYS_LEAK`, are new to that path. The composite is selected by `AEGIS_STREAMING_ENGINE`, which defaults to `grammar_frontier`; setting `deidentifier` restores the pre-`4.3.0` output exactly. **Response redaction off means off**: with both PHI and PCI detector families disabled the frontier stage does not run either, so a deployment that switched redaction off keeps byte-for-byte pass-through and acquires no holdback. The composite withholds the de-identifier window plus the 28-character frontier and reports that sum as `W`, so the per-stream ceiling in `CLM-078` still covers everything the stream retains. Measured per-chunk overhead is +10 to +12 µs at p50 and +3.8 to +22.7 µs at p99 across five runs — the p99 figure is at this harness's noise floor and must not be quoted as a measurement of the stage. It is deterministic pattern matching over four declared patterns and nothing else: it establishes no guarantee that adversarial text is absent, no semantic understanding, and no protection against an encoding it has no pattern for; prompt-injection prevention remains an open problem (`CLM-024`). The frontier is a real bound only because each pattern's whitespace runs are bounded, and the cost is one-directional and asserted by test: an evasion padding whitespace past the bound is not matched, the same trade the `ADDRESS` bound makes in `CLM-057`. Wiring adds patterns, not understanding: a paraphrase, a translation, or an encoding the four rules have no pattern for passes through exactly as before. |
| `CLM-068` | `CryptoShredder` seals a payload under a per-subject AES-256-GCM key from the `cryptography` library and commits the ciphertext, not the plaintext, so destroying that key renders the plaintext unrecoverable to a holder of the ciphertext while the MMR root, its peaks and previously issued inclusion proofs remain bit-for-bit unchanged. | `IMPLEMENTED` and wired into the ledger commit path as an **opt-in that is off by default**; `LOCALLY TESTED` | `aegis/core/crypto_shredder.py`; `aegis/core/crypto_audit.py` (`_seal_leaf`, `crypto_shred`, `open_sealed_leaf`); `tests/test_crypto_shredder.py` (`TestTreeInvariance`); `tests/test_crypto_shredder_integration.py` | **Off by default** (`AEGIS_ENABLE_CRYPTOGRAPHIC_SHREDDING=false`), so an unchanged deployment commits payload digests directly and behaves exactly as before. It is **not a flag that can be turned on for a chain that already has records**: sealing changes what the MMR commits to — the leaf becomes `SHA-256(0x00 \|\| nonce \|\| ciphertext)` rather than the payload digest — so enabling it means starting a new chain, the same migration rule `CLM-064` states for the hash scheme. When on, the WAL carries ciphertext rather than digests alone, so its growth tracks payload size, and the key vault (`<wal_path>.shredder.db` by default) becomes the only mutable component in an append-only design: lose it and every plaintext is gone at once, restore it from backup and every erasure through it is undone. `crypto_shred(subject_id)` is idempotent and returns whether a key was destroyed. The unrecoverability claim is bounded to a holder of the ciphertext at the strength of AES-256-GCM; it is **not** a guarantee that key material is gone from the physical medium. Allocator copies, the SQLite journal, filesystem journaling, page cache, swap, snapshots, backups, replicas, and SSD wear-levelling are all outside a Python process's control, and the `bytearray` scrub is hygiene against a casual memory scrape rather than media sanitisation. A shredded node **still carries its request and response digests**, so a guessed plaintext can be confirmed against them. A deployment needing a destruction guarantee must hold keys in an HSM or a KMS with key deletion and obtain target acceptance. **No regulatory conclusion follows**: this is a mechanism, it is not advice, and no statement of compliance with any specific regulation is made or implied (`CLM-039`, `CLM-042`). |
| `CLM-069` | `aegis/licensing/validator.py` verifies an Ed25519-signed entitlement token offline: the signature is checked before the payload is parsed, and expiry, module grants and field types are validated. Every failure raises a `PermissionError` subclass. | `IMPLEMENTED` in the v4 source baseline; `LOCALLY TESTED` | `aegis/licensing/validator.py`; `scripts/generate_commercial_license.py`; `tests/licensing/test_license_validator.py` | **There is no compiled-in root public key**: it must be supplied or set in `AEGIS_LICENSE_ROOT_PUBKEY`, and an absent key refuses rather than defaulting — a 32-byte placeholder loads as a valid Ed25519 key, so a hardcoded fallback would fail silently rather than loudly. A token is a **bearer credential**: copying it copies the entitlement, and offline licensing cannot bind to a machine without a phone-home or a hardware root, neither of which exists here. Expiry is checked against the host clock, so a rolled-back clock extends a licence undetectably. `max_annual_mgt` is **carried, not enforced** — nothing counts or caps transactions. There is no revocation: no CRL, no OCSP, and revoking before expiry requires rotating the root key and reissuing. |
| `CLM-070` | `aegis/engines/` exposes four facades — Veracity, Sanctum, Agentis, Sovereign — over existing core modules, usable as libraries without running the HTTP gateway. | `IMPLEMENTED` in the v4 source baseline; `LOCALLY TESTED` | `aegis/engines/{veracity,sanctum,agentis,sovereign}.py`; `tests/engines/test_modular_engines.py` | They **add no capability and relax no boundary**: every limit on the wrapped component applies unchanged, including that proofs are non-zero-knowledge against a separately trusted root (`CLM-006`), that redaction is finite pattern matching rather than de-identification (`CLM-016`, `CLM-057`), that a receipt attests to a record and not to an execution, and that no constant-time property is claimed for ML-DSA. Licence gating is **off by default** and does not restrict the AGPLv3 build; `AEGIS_LICENSE_ENFORCEMENT=required` is opt-in. `SovereignVault.capabilities()` probes each optional backend at runtime and reports what is actually present. |
| `CLM-071` | `aegis/connectors/` carries evidence copies into Splunk HEC, a Parquet lakehouse and HashiCorp Vault Transit, with bounded buffering and failure isolation. | `IMPLEMENTED` in the v4 source baseline; `LOCALLY TESTED` | `aegis/connectors/siem/splunk_hec.py`; `aegis/connectors/lakehouse/parquet_exporter.py`; `aegis/connectors/vault/transit_signer.py`; `tests/connectors/` | Connector output is a **derivative copy, never the evidence**: the JSONL WAL stays the replay authority and verification is against it, never against a Parquet table or a Splunk index. Parquet is a **projection** — hashes and roots are carried, bodies are not — and engine-side encoding, coercion or compaction can change bytes without changing SQL semantics. Splunk delivery is **at-least-once**, so duplicates are expected; a full queue drops rather than blocking, and an over-bound spool discards oldest-first, both counted. Vault Transit moves **key custody, not trust**: it signs for whoever holds a token carrying the policy, which is not non-repudiation, and it places Vault's availability on the commit path. `pyarrow` is an optional extra. |
| `CLM-072` | `connectors/envoy-wasm` builds a `proxy-wasm` filter for Envoy/Istio that refuses declared critical request patterns and redacts declared identifier patterns from response chunks behind a bounded holdback, running redaction to a fixpoint first. | `IMPLEMENTED` in the v4 source baseline; `LOCALLY TESTED` | `connectors/envoy-wasm/Cargo.toml`; `connectors/envoy-wasm/src/lib.rs` (7 unit tests); builds to `wasm32-wasip1` | It **commits no evidence** — no ledger, no MMR, no WAL, no signature — so a deployment running only this filter has request governance and no evidence trail. Its pattern set is a **subset** of the gateway's: literal ASCII byte matching on the body as received, with no Unicode normalisation, no zero-width stripping and no nested-JSON walk. It prevents no prompt injection (`CLM-024`). The holdback is a real bound only because every pattern length is finite, and the cost is one-directional: a pattern padded past the frontier is not matched. No latency or throughput measurement exists for it, so no performance claim may be made. |
| `CLM-073` | `scripts/triage/dependency_triage.py` reads every dependency the repository locks across the Python, Rust and npm trees, queries OSV for each pinned version, and assigns each to exactly one of six action buckets under a fixed precedence. | `IMPLEMENTED`; `MEASURED` on 2026-09-09 (557 components) | `scripts/triage/dependency_triage.py`; `docs/security/DEPENDENCY_TRIAGE.md`; `docs/security/advisory_snapshot.json` | Absence of an advisory means **OSV held none for that exact version on the snapshot date** — not that a component is free of vulnerabilities, and not an external assurance. The scan reads lock files, so a defect a hosted scanner sees by other means is invisible to it. `BUCKET-2` rests on a curated register, and rows resting on an unreproduced external claim are marked as such. Advisories with no fixed version are routed to `BUCKET-5`, not `BUCKET-1`: they are maintenance findings with no bump available, and three of the four found sit under the ML-DSA signing path because upstream PQClean is being archived. **The two scan exports this review was commissioned against were not present in the repository or the session, so their rows are absent from the report.** |
| `CLM-074` | `scripts/license/license_scan.py` inventories the declared licence of every distributed component across all three ecosystems, reading installed metadata, crate manifests and lock files, and emits `LICENSE-THIRD-PARTY.md` and a tier-compatibility matrix. | `IMPLEMENTED`; `MEASURED` on 2026-09-09 (380 distributed components: 365 permissive, 15 weak copyleft, 0 strong copyleft, 0 unknown) | `scripts/license/license_scan.py`; `docs/compliance/LICENSE_AUDIT.md`; `LICENSE-THIRD-PARTY.md` | This is an **inventory, not legal advice and not a compliance certification**. It reports the identifiers upstream projects declare; it does not verify that a declared identifier matches the licence text in the package, and it does not resolve compatibility questions that turn on how a work is combined. No output is an opinion on whether any distribution is lawful. The 15 weak-copyleft components are the optional per-platform `@img/sharp-libvips-*` binaries reached through the dashboard's Next.js image path plus `certifi` (MPL-2.0); the source offer in `LICENSE-THIRD-PARTY.md` is a stated undertaking, not an executed arrangement. |
| `CLM-075` | The Rust digest stack was migrated from `sha2` 0.10 / `hmac` 0.12 to 0.11 / 0.13, removing `block-buffer 0.10.4` from the tree, with hash output unchanged byte for byte. | `IMPLEMENTED`; `MEASURED` (49 Rust tests, 106 Python MMR parity tests under `AEGIS_REQUIRE_RUST=1`, Miri clean on the digest tests) | `aegis_rust_v2/Cargo.toml`; `aegis_rust_v2/src/ledger.rs` (FIPS 180-4 and RFC 4231 vector tests) | Byte identity is asserted against **externally defined standard test vectors** — FIPS 180-4 for SHA-256, RFC 4231 for HMAC-SHA-256 — not against the implementation's own prior output, and full digests are compared rather than prefixes. This **remediates a real advisory**: scanner row `0.31` reports `block-buffer 0.10.4` at CVSS 6.3 with an **empty CVE ID field**, which is why an OSV query returns nothing and why an earlier revision of this row wrongly recorded `[UNKNOWN_MISSING_PRIMARY_SOURCE]`. The defect is a caught panic leaving a buffer cursor violating its invariant, after which `get_pos()` reaches `unreachable_unchecked`. `aegis_rust_v2/tests/block_buffer_panic_safety.rs` ports both proofs the advisory itself carries, plus a third shape and passes under `cargo miri test`, so the fix is demonstrated on the linked version rather than inferred from a version number. Boundary: three panic shapes on one version under Stacked Borrows is **not** a proof that the crate is free of unsoundness, and Miri covers only executed paths — not the PyO3 or WAL boundaries. |
| `CLM-076` | `tests/redteam/` attacks the de-identifier, the streaming frontier and portable proof verification with hostile input; two defects found this way were fixed and pinned. | `IMPLEMENTED`; `MEASURED` (146 red-team tests; 100,000 and 200,000 fuzz executions) | `tests/redteam/`; `tests/redteam/fuzz/`; `evidence/fuzz/4.3.0/README.md`; `aegis/core/phi_deidentifier.py` (EMAIL bound); `aegis/core/mmr.py` (proof field types) | Two real defects, both now regression-tested: the `EMAIL` pattern's unbounded local-part run was **quadratic** on separator-dense input (measured 12.29s for 40 KB, 73.8ms after bounding it at the RFC 5321 limits), and `MMRInclusionProofV1.from_dict` accepted a non-integer `leaf_count`, making the verifier **raise instead of returning `False`**. Neither finding implies the classes are now absent: bounded scaling is asserted for the shapes tested, and a fuzzing campaign that finds no crash establishes only that these harnesses at these execution counts found none. The campaigns did **not** reach saturation, and the Rust parsers are **not** fuzzed at all — `cargo-fuzz` could not be installed in this environment. |
| `CLM-077` | The licence entitlement model lives in `aegis/licensing/model.py`, separate from token decoding and signature verification, and its term checks accept an explicit `now` in epoch seconds. | `IMPLEMENTED`; `LOCALLY TESTED` | `aegis/licensing/model.py`; `aegis/licensing/validator.py`; `tests/licensing/test_license_model.py` | Supplying `now` makes the **expiry boundary testable**; it does not make expiry trustworthy. The value still comes from the caller, offline licensing has no trusted time source, and a host whose clock runs backwards extends its own licence — unchanged from `CLM-069`. Omitting `now` reads `time.time()`, which is what the gateway does. The split is a statement about **this module's own imports**, not about what an `import` loads: importing the model initialises the `aegis.licensing` package, which re-exports the verifier and therefore does load a crypto backend. Every previously public name resolves to the same object from either import path. |
| `CLM-078` | The per-stream retained-byte ceiling `R_max = 4W + Q + E + P` declared in `specs/aegis_stream_buffer.smt2` is computed in code by `aegis/core/stream_bounds.py`, and `SanctumEngine` fails closed when its holdback exceeds it. | `IMPLEMENTED`; `LOCALLY TESTED` | `aegis/core/stream_bounds.py`; `aegis/engines/sanctum.py`; `aegis/proxy/streaming.py` (`bounds`, `retained_bytes_ceiling`); `tests/test_stream_bounds.py`; `tests/engines/test_modular_engines.py` | This connects the expression to the running code; it does **not** upgrade what the Z3 run establishes. That check remains arithmetic consistency over declared ranges — asserting the expression and then asserting it exceeds itself — and is **not** a refinement proof of the proxy, of CPython's allocator, or of process memory, exactly as `docs/formal/FORMAL_VERIFICATION_LIMITS.md` records. `4W` is a **per-stream** ceiling: aggregate memory still scales with concurrent admitted streams and remains `CONFIGURATION-DEPENDENT` on deployment admission control. In-process there is no queue, canonical event or preview, so `Q`, `E` and `P` are genuinely zero and the configuration sits **outside** the spec's declared ranges, which assume a proxied stream; the code reports that rather than inventing a queue budget. The proxy accessor is **reporting only** — it changes no admission decision, so it cannot reject a stream the previous release admitted. |
| `CLM-079` | `tests/compat/` asserts that the console entry points, HTTP routes, Python exports and Rust FFI names present at the published `v4.1.2` tag are still present. | `IMPLEMENTED`; `LOCALLY TESTED` | `tests/compat/test_public_api_compat.py` | The baseline is `v4.1.2` because it is the **most recent published release**; there is no `4.2.0` at any surface, so no `4.2.0` contract exists to compare against. The assertions are **one-directional subsets**: a name or route that existed then must exist now, and additions are permitted. It pins **names, routes and call shapes only** — not response bodies, not persisted-evidence wire compatibility (covered by `CLM-064` and the MMR parity tests), and nothing about the SDKs, which version independently. Passing it is not a compatibility guarantee for behaviour that changed underneath an unchanged signature. |
| `CLM-080` | `GossipDaemon` reconciles the `CausalMmr` accumulator across replicas by anti-entropy over mutual TLS: each round compares roots and, when they differ, exchanges encoded leaf sets in both directions. Replicas that can reach one another converge on one root, and a replica that was partitioned converges once the partition heals. | `IMPLEMENTED` and started by the gateway's own lifespan behind an opt-in that is **off by default**; `LOCALLY TESTED` against three replicas on real TLS sockets | `aegis/consensus/gossip.py`; `aegis/consensus/transport.py`; `aegis/consensus/runtime.py` (`start_gossip`, lifespan wiring); `aegis/proxy/app.py` (started before the seccomp filter, stopped first on shutdown); `aegis_rust_v2/src/crdt_mmr.rs` (`encode_state`, `decode_state`); `tests/consensus/test_gossip_convergence.py`; `tests/consensus/test_gossip_settings.py`; `tests/consensus/test_gossip_runtime.py` | **It reconciles the CRDT accumulator, not the ledger.** Each replica's WAL remains its own chain and no receipt, root or inclusion proof the ledger issues is affected by enabling it (`CLM-012` unchanged). It is **not consensus**, not Byzantine fault tolerance, and not a total order over the ledger: `join` reconciles replicas that disagree about *ordering*, not replicas that *lie*, and a replica that contributes fabricated leaves has them merged like any other. Mutual TLS establishes that a peer holds a certificate the operator issued — nothing about the truth of what it sends. There is **no membership protocol**: the peer set is static configuration, so adding, removing or renaming a replica is an operator action. A full exchange is O(state), so wire volume grows with history; the root-digest comparison is what keeps that off the converged path. Convergence is asserted over three replicas on loopback TLS with a simulated partition and heal — that is not a multi-region, multi-node, or capacity result, and no convergence-latency figure is claimed. The decoder is bounded and total (leaf-count ceiling, canonical clock ordering, every truncation refused), which resists a hostile peer exhausting memory; it is not a claim that a hostile peer cannot waste bandwidth. |
| `CLM-081` | Two concurrent appenders cannot fork the enterprise storage chain. `write_node_atomic` appends only when the tip is still the one the caller read, and a uniqueness constraint on `prev_hash` makes two nodes naming one predecessor unrepresentable rather than merely detectable. | `IMPLEMENTED` and wired into the analytics append path; `LOCALLY TESTED` on SQLite, where the fork was **reproduced** before the fix — ten concurrent appends produced ten nodes all naming one predecessor | `aegis_server/storage/base.py` (`write_node_atomic`, `ConcurrentChainMutationError`); `aegis_server/storage/sqlite_provider.py`; `aegis_server/storage/postgres_provider.py`; `aegis_server/storage/dynamodb_provider.py`; `aegis_server/main.py` (`_CHAIN_APPEND_LOCK`); `tests/storage/test_chain_fork_prevention.py` | **Only the SQLite path is verified here.** The PostgreSQL and DynamoDB implementations are written against their documented concurrency primitives — a `FOR UPDATE` compare-and-append with a unique partial index, and `TransactWriteItems` against a tip item — but `asyncpg`, a PostgreSQL server, and a DynamoDB endpoint (or `moto`) are all absent from this environment, so neither was executed. Treat both as `CONFIGURATION-DEPENDENT` pending a run against a real backend. This governs the **enterprise storage providers**, not the JSONL WAL, which is a separate path with its own single-writer advisory lock (`CLM-056`). A refusal is reported to the caller and the node is **not** written: the analytics path does not retry, because a retry would have to re-add an MMR leaf and this process's MMR is a separate accumulator from the shared chain — so an append refused by another *process* is lost rather than silently reconciled, and that gap is real until the MMR and the store commit together. The in-process lock covers one worker; it establishes nothing across workers or hosts, where the storage-layer guard is the only protection. Enabling the constraint on a store that **already** forked refuses to initialise rather than repairing it, because no rule here can decide which branch is the record. |
| `CLM-082` | Concurrent WAL commits are coalesced onto a shared `fsync`: records are written and ordered under the ledger lock, and one `fsync` — issued with that lock released — makes every record written while it was in flight durable. A commit does not return until its own record is covered by a completed `fsync`. | `IMPLEMENTED`, on by default, and `LOCALLY TESTED`; `MEASURED` in one environment | `aegis/core/group_commit.py`; `aegis/core/crypto_audit.py` (`_persist_node`, `_sync_wal`, `_await_durable`, `_rotate_wal_locked`); `tests/test_coalesced_commit.py`; `tools/benchmarks/run_group_commit.py` | **The per-record durability contract is unchanged; the ordering around it is not.** A node now enters the in-memory chain once its bytes are written and ordered, *before* its batch is synced, because the next committer must be able to read the tip to link against it. Nothing observes that node in the meantime — the commit call has not returned — and a crash in that window loses the in-memory chain too, so replay rebuilds a shorter, internally consistent chain. The older claim that each node is fsynced *before* the chain is updated no longer holds and must not be repeated. **Failure is collective and terminal**: a failed `fsync` fails every commit waiting on it and latches `wal_persist_failed`, which `_require_intact_ledger` reads to answer 503; individual nodes are deliberately not unwound, because later nodes have already linked against them. `AEGIS_COMMIT_BATCH_MAX_SIZE` bounds how long a syncer waits for company — it cannot bound what an `fsync` covers, since `fsync` is not scopable to part of a file, so the observed batch size may exceed it. This claims nothing `fsync` itself does not deliver: a device or virtualisation layer that acknowledges a sync before the write is persistent defeats this exactly as it defeated the per-record version. The measured effect — 1.44x commit throughput and p99 55.5 ms to 24.8 ms over 400 commits across 32 threads, 400 `fsync` calls reduced to 66 — is **one machine's filesystem**, recorded by `tools/benchmarks/run_group_commit.py`, and is not a capacity or SLA figure for any deployment. Concurrency in that measurement comes from a thread pool standing in for the proxy's `asyncio.to_thread` commit path, not from that path. |
| `CLM-083` | `verify_cluster_receipt` verifies an A2A receipt against a supplied set of per-replica **ledger** roots and returns the name of the replica whose root verifies it, or `None`. | `IMPLEMENTED`; `LOCALLY TESTED` | `aegis/core/a2a.py` (`verify_cluster_receipt`); `tests/test_a2a_protocol.py` | **A match is one replica's word, not the cluster's.** It establishes exactly what `CLM-044`/`verify_receipt` establishes against that one root, and nothing more: replicas do not attest to each other's ledgers, so this is not a quorum, not corroboration, and not agreement. **The roots are ledger roots, not gossip roots** — the mesh in `CLM-080` reconciles the `CausalMmr` accumulator, a different structure with a different root, and passing one here returns `None` for a reason that has nothing to do with the receipt's validity. The independent-root requirement is unchanged and is not diluted by searching several: a root set collected from whoever supplied the receipt proves internal consistency only. A `None` result means no supplied root verified the receipt — it does not distinguish a forged receipt from a root set that simply omitted the issuing replica. |
| `CLM-084` | The ML-DSA-65 implementation sits behind a `PostQuantumSigner` trait, so the archived PQClean backend can be replaced by the pure-Rust `ml-dsa` crate through a Cargo feature without changing the PyO3 class, its methods, or the exported FFI symbols. | `IMPLEMENTED`; `LOCALLY TESTED` in three build configurations; `MEASURED` in one environment | `aegis_rust_v2/src/pqc_trait.rs`; `aegis_rust_v2/src/pqc.rs`; `aegis_rust_v2/Cargo.toml` (`pqclean-pqc`, `pure-rust-pqc`, `pqc-compat-tests`); `docs/security/DEPENDENCY_RISK_REGISTER.md`; `docs/security/PQC_CONSTANT_TIME.md` | **The default build is unchanged and still uses PQClean.** This claims a seam and a proven-compatible alternative, not a migration. Under `--no-default-features --features pure-rust-pqc` the dependency tree contains `ml-dsa` and none of `pqcrypto-mldsa`, `pqcrypto-traits`, `pqcrypto-internals` or `paste`, so the swap retires `RUSTSEC-2026-0166`, `-0162`, `-0163` and `RUSTSEC-2024-0436` — none of which is a defect; all four are unmaintained-status notices. Compatibility is asserted by the `cross_backend` tests, which establish that each backend verifies the other's signatures and that the pure-Rust backend derives the same public key from the persisted 4032-byte secret, so no existing evidence is stranded and a rollback stays open; they do **not** establish FIPS 204 conformance, which needs the standard's own vectors, nor equivalence on inputs outside those the tests generate. The pure-Rust backend loads persisted identities through an upstream-deprecated API that upstream warns can panic on a malformed key; the length check ahead of it is what a truncated file meets, and the file is operator-provisioned. Costs are `MEASURED` on one machine — verify 1.43x, sign 4.80x — and are not a capacity figure. **No constant-time claim is made or lifted for either backend**; see `CLM-019` and `PQC_CONSTANT_TIME.md`, where the release gate stands closed. |
| `CLM-085` | The digest buffer beneath every SHA-256 and HMAC call on the evidence path is executed under Miri and reports no undefined behaviour when its compression function panics. | `IMPLEMENTED`; `LOCALLY TESTED` and gated in CI | `aegis_rust_v2/tests/block_buffer_panic_safety.rs`; the `Miri Undefined Behaviour` CI job (`cargo miri test --test block_buffer_panic_safety`, pinned `nightly-2026-09-09`); [Formal Verification Limits §Miri](formal/FORMAL_VERIFICATION_LIMITS.md) | **Miri is an interpreter, not a prover**, so this covers only the paths the test drives — three panic sites in one buffer — and establishes nothing about the crate as a whole. Three things put the rest out of reach and all three are Miri's properties rather than choices: it cannot execute foreign functions, and the default build links PQClean's C ML-DSA; it does not service `mmap`, so the WAL's own paths are unreachable (their bounds arithmetic is `CLM-054`'s subject instead); and `--lib` pulls tokio, reqwest and vendored OpenSSL through Miri's compilation, which had not finished after fifteen minutes when measured. A pass means *no undefined behaviour was observed on these paths, under this interpreter, at this pinned toolchain* — not that the crate is free of it. Nightly is not a preference: Miri ships only as a nightly component, so no stable configuration of this gate exists. |
| `CLM-086` | `aegis/core/tee_backends.py` supplies three `AttestationVerifier` implementations. `NitroBackend` and `SevSnpBackend` refuse in every case; `RecordedAttestationBackend` replays captured claims without authenticating them, and a replayed document cannot satisfy `AttestationPolicy` under a fresh nonce or a real clock. | `IMPLEMENTED`; `LOCALLY TESTED` | `aegis/core/tee_backends.py`; `tests/test_tee_backends.py`; `aegis/core/tee_manager.py` (`evaluate_attestation_claims`) | **Nothing in this tree performs attestation, and these backends do not change that.** Both platform adapters refuse unconditionally, and they refuse in two distinguishable ways on purpose: no device means the workload is not on the platform it believes it is, while a present device means the platform is right and the vendor verifier is what is missing. Neither reads a COSE_Sign1 document, an SNP report, a certificate chain, a PCR set or a TCB version, because no such verifier is integrated; device presence is discovery evidence, the position `CLM-019` and `TEEManager.initialize_enclave` already take. `RecordedAttestationBackend` authenticates nothing at all — it ignores the evidence bytes and returns a fixture — so a test that passes through it exercises the **policy path** and establishes no attestation whatever. That it cannot escape into production is structural rather than advisory: the recorded nonce and issue time are frozen, while production issues a fresh nonce per challenge and reads a real clock, so `evaluate_attestation_claims` rejects the replay on `compare_digest` and on `max_age`. Its `bind_nonce` option deliberately defeats that check and exists only so policy-field rejection can be tested without the nonce masking it. No claim is made that a quote was generated, verified, or could be. |
| `CLM-087` | `aegis/core/pqc_tls.py` performs a hybrid key exchange composing X25519 ECDH with ML-KEM-1024 (FIPS 203), deriving the session secret as `HKDF-SHA256(x25519_ss ‖ mlkem_ss)`. Two parties independently derive the same 32-byte secret. When the ML-KEM backend is absent it raises `HybridKEMUnavailableError` rather than downgrading to classical-only. | `IMPLEMENTED`; `CONFIGURATION-DEPENDENT` — inert unless the `pqc` extra is installed; `LOCALLY TESTED` | `aegis/core/pqc_tls.py`; `aegis/core/mlkem_session.py`; `aegis/engines/sovereign.py`; `tests/test_optional_backend_declarations.py` | **This is not TLS.** There is no TLS listener, no `SSLContext`, and no cipher-suite or named-group negotiation anywhere in this module: it is an application-layer key exchange between two parties that already have a channel. It must not be described as "PQC TLS", "PQC TLS ingress", "quantum-safe TLS" or "hybrid TLS" — the gateway's own ingress TLS is terminated by the customer and is outside this repository entirely. **The parameter set is ML-KEM-1024, not the TLS hybrid group `X25519MLKEM768`**, which the module docstring cites only as an analogy for the composition; the two are different parameter sets and must not be interchanged. The post-quantum half is `kyber-py`, a pure-Python implementation: **no constant-time, side-channel-resistance or FIPS-validation property is claimed for it**, and none should be inferred from FIPS 203 naming a standard the library implements. Nothing on the gateway request path uses this exchange; it is reached through the Sovereign engine facade and the `aegis.crypto` re-export. Hybrid composition means the secret stands as long as **either** primitive is unbroken — that is a property of the construction, not a measurement, and no interoperability with any TLS stack is claimed or tested. |
| `CLM-088` | A governed request that reaches a durable commit records `waf_verdict` (`passed`) in the bytes the MMR commits to, and the node signature binds that value. Nodes written without a verdict are byte-identical to those written before the field existed. | `IMPLEMENTED`; `LOCALLY TESTED` | `aegis/core/forensic.py` (`build_merkle_leaf`, `validate_waf_verdict`); `aegis/core/crypto_audit.py` (`_build_signed_payload`, `AuditNode.waf_verdict`); `aegis/proxy/app.py`; `tests/test_waf_verdict_schema.py` | **It records that the WAF allowed the request; it does not establish that the WAF is correct.** A blocked request raises before the durable commit, so reaching one implied a pass already — this field makes that implication *recorded* rather than *inferred*, and nothing more. It says nothing about coverage gaps, misconfiguration, a bypass upstream of the check, semantic safety, or whether the payload was in fact benign. The vocabulary is closed (`passed`, `blocked`, empty) and delimiter-free, which is load-bearing rather than tidy: the signed payload is `"|".join`, so an unconstrained verdict would be a serialisation ambiguity. Empty means **not recorded**, not a third outcome. The field is **omitted from the leaf when empty**, so every pre-existing leaf, signature and issued proof is unaffected, and A2A receipts — the only envelope both SDKs reconstruct — carry no verdict and are byte-identical, so **no SDK wire boundary is created**. It is deliberately **not** a `node_hash` input, for the same reason the sealing fields are not. This records the verdict; it does **not** by itself prove anything in zero knowledge. |

## Claim control register

Governance metadata for each claim, keyed by ID. The register is split from the claim table above so both stay readable; together they carry claim, state, evidence locator, boundary, forbidden phrasing, review date, and owner.

Forbidden phrasing is additive to the global prohibitions in [Style Guide §3](STYLE_GUIDE.md#3-prohibited-language), which apply to every claim without being repeated here.

| ID range | Domain | Do not use, in this domain | Last reviewed | Owner |
|---|---|---|---|---|
| `CLM-001`–`CLM-008` | Gateway surface, streaming, SDKs, forensic export, dashboard | "complete audit trail", "tamper-proof", "every request is proven" | 2026-09-02 | Architecture owner |
| `CLM-009`–`CLM-013` | Ledger integrity, WAL topology, storage adapters, enforcement posture | "immutable", "WORM", "cannot be altered", "guaranteed durability" | 2026-09-02 | Architecture owner + Platform/SRE owner |
| `CLM-014`–`CLM-020` | Anchoring, identity, SIEM, forensic query, capability reporting, TEE | "trusted timestamp", "attested hardware", "non-repudiation" | 2026-09-02 | Security owner |
| `CLM-021`–`CLM-026` | Differential privacy primitive, HSM, fuzzing readiness, AI context pack, publication workflows | "differentially private product", "FIPS validated", "fuzzed and clean" | 2026-09-02 | Security owner + Release owner |
| `CLM-027`–`CLM-032` | Formal artifacts, signer policy, keyring, PQC | "proven correct", "quantum-safe", "post-quantum secure" | 2026-09-02 | Architecture owner + Security owner |
| `CLM-033`–`CLM-038` | Egress guard, WAF corpus, backpressure, throughput, microbenchmark | "blocks all attacks", "10k RPS in production", "zero overhead" | 2026-09-02 | Security owner + Performance owner |
| `CLM-039`–`CLM-042` | Scaling, samples, regulatory contribution, support and pricing | "compliant", "certified", "court-admissible", "enterprise SLA", "validated pricing" | 2026-09-02 | Release owner + Privacy/legal owner |
| `CLM-043`–`CLM-047` | Durability, proof semantics, WAL authority, streaming state, redaction | "power-loss safe", "zero-knowledge proof", "removes all PII", "guaranteed redaction" | 2026-09-02 | Architecture owner + Privacy/legal owner |
| `CLM-048`–`CLM-050` | Publication and provenance state | Do not use: "the gateway ships from source only"; "the registries carry SDKs only"; "PyPI/npm (gateway): not applicable"; "published 4.0.2 to a registry"; "the SDK is on npm at 4.1.1"; "signature verified by GitHub"; "attestations verified"; "the PyPI gateway wheel is covered by SHA256SUMS". The first three were accurate at `4.1.1` and are false at `4.1.2`; the rest are not established by readback at any version. State PyPI and npm separately, and never restate a readback with the version changed. | 2026-09-04 | Release owner |
| `CLM-051`–`CLM-053` | Service levels and storage immutability | "SLA", "uptime guarantee", "immutable", "WORM" | 2026-09-02 | Release owner + Platform/SRE owner |
| `CLM-054`–`CLM-059` | Verified mechanism properties | Do not use: "the WAL is memory-safe"; "formally verified"; "proven crash-safe"; "zero memory leaks"; "guaranteed single writer" — advisory locks are cooperative, and the Kani proofs cover two arithmetic functions, not a system | 2026-09-03 | Release owner + qualified formal-methods reviewer |
| `CLM-060`–`CLM-063` | Rejection evidence, embedded mode, A2A receipts, peak restore | Do not use: "blocks are always recorded"; "the embedded mode sandboxes the application"; "a receipt proves the tool ran"; "receipts authenticate agents"; "arguments are encrypted"; "restore is verified continuity" — a rejection commit can fail while the block still holds, the embedded engine is bypassable from inside its own process, and a receipt proves inclusion only | 2026-09-03 | Release owner + Architecture owner |

## Required wording controls

Use **“implements controls aligned with”** instead of **“satisfies compliance.”** Use **“timing leakage was not detected under the named experiment”** instead of **“constant-time.”** Use **“offered load”** instead of **“capacity.”** Use **“independently verifiable per-replica evidence bundle”** instead of **“globally ordered multi-region audit trail.”** Use **“static demo telemetry”** instead of **“live telemetry”** for material under `Samples/`.

## External reference boundary

Framework references are contribution mappings and review lenses. NIST AI RMF is a voluntary framework, and ISO/IEC 42001 is an AI management-system standard; neither reference certifies this repository. Primary sources for the v3.1.0 baseline are registered in `evidence/market_hardening_v3_1/documentation_source_register_v3_1_0.md`. Framework mappings do not determine legal applicability or replace a customer assessment.

## Falsification protocol

A public claim is blocked when its source path changes, the named regression or benchmark fails, the workload boundary changes without rerun, a deployment prerequisite is absent, an independent reviewer identifies a contradiction, or a customer-facing document uses stronger language than this matrix. Every future claim must add a locator, evidence artifact, boundary conditions, and a falsification test before publication.

## Machine-readable source

The original baseline scan contained 791 claim-bearing lines and 79 drift candidates. The generated v3.1.0 ledger contains 304 claim records and 27 drift candidates in `claims_ledger_v3_1_0.json`; it is retained as a release artifact outside the source tree. The scan is an inventory aid; human review remains required for context, reachability, legal interpretation, source quality, and the effect of this documentation reconstruction.

## Related documents

- [`README.md`](../README.md)
- [`docs/compliance/COMPLIANCE_MAPPING.md`](compliance/COMPLIANCE_MAPPING.md)
- [`docs/privacy/DATA_RETENTION.md`](privacy/DATA_RETENTION.md)
- [`docs/benchmarks/BENCHMARK_RESULTS.md`](benchmarks/BENCHMARK_RESULTS.md)
- [`docs/security/THREAT_MODEL.md`](security/THREAT_MODEL.md)
- [`docs/security/PQC_CONSTANT_TIME.md`](security/PQC_CONSTANT_TIME.md)
